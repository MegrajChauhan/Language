#include "global_context.h"

bool populate_keys()
{
    // key_maps = umap_create(KEYWORD_COUNT, slice_hash_str, slice_cmp, slice_destroy, )
    // keys = umap_create(KEYWORD_COUNT, slice_hash_str, )
}