#ifndef _MAPS_
#define _MAPS_

#include "umap.h"
#include "defs.h"
#include "slice.h"

static umap *key_map = NULL;
static umap *oper_map = NULL;

// bool gen_map();

// bool find_key(slice *s, uint64_t *_s);

// bool find_oper(slice *s, uint64_t *_s);

// void rid_of_map();

#endif